function Todo(text, id, status) {
    this.text = text, this.id = id, this.status = status;
}

Todo.prototype.check = function() {
    this.status = !this.status;
};

for (var Model = {
    id: 0,
    find: function(id) {
        for (var i = 0; i < this.todos.length; ++i) if (this.todos[i].id === todoID) return this.todos[i];
        return null;
    },
    push: function(text) {
        this.todos.push(new Todo(text, ++this.id, !1));
    },
    pop: function(item) {
        this.todos.splice(this.todos.indexOf(item), 1);
    },
    check: function(id) {
        return null !== this.find(id);
    }
}, model = Object.create(Model, {
    todos: {
        value: []
    }
}), i = 1; 10 != i; ++i) model.push(i);

var TodoItem = React.createClass({
    displayName: "TodoItem",
    onCheck: function() {
        this.props.onCheck(this.props.model);
    },
    onRemove: function() {
        this.props.onRemove(this.props.model);
    },
    render: function() {
        return React.createElement("a", {
            href: "#",
            className: "list-group-item",
            onClick: this.onCheck
        }, React.createElement("span", {
            className: this.props.model.status ? "my-todo-checked" : ""
        }, this.props.model.text), React.createElement("button", {
            type: "button",
            className: "close",
            "aria-label": "Close",
            onClick: this.onRemove
        }, React.createElement("span", {
            "aria-hidden": "true"
        }, "×")));
    }
}), Input = React.createClass({
    displayName: "Input",
    onSubmit: function(e) {
        e.preventDefault();
        var text = React.findDOMNode(this.refs.newTodo).value.trim();
        React.findDOMNode(this.refs.newTodo).value = "", text && this.props.onSubmit(text);
    },
    render: function() {
        return React.createElement("form", {
            className: "input-group",
            onSubmit: this.onSubmit
        }, React.createElement("input", {
            type: "text",
            className: "form-control",
            placeholder: "New item",
            ref: "newTodo",
            autofocus: !0
        }), React.createElement("span", {
            className: "input-group-btn"
        }, React.createElement("button", {
            className: "btn btn-default",
            type: "submit"
        }, "Add")));
    }
}), Container = React.createClass({
    displayName: "Container",
    addItem: function(text) {
        this.props.model.push(text), this.forceUpdate();
    },
    removeItem: function(item) {
        this.props.model.pop(item), this.forceUpdate();
    },
    checkItem: function(item) {
        item.check(), this.forceUpdate();
    },
    render: function() {
        var self = this, items = {
            done: [],
            pending: []
        };
        return this.props.model.todos.forEach(function(item) {
            items[item.status ? "done" : "pending"].push(React.createElement(TodoItem, {
                model: item,
                onCheck: self.checkItem,
                onRemove: self.removeItem,
                key: item.id
            }));
        }), React.createElement("div", {
            className: "well my-holder"
        }, React.createElement("div", {
            className: "list-group"
        }, items.pending), React.createElement("div", {
            className: "list-group"
        }, items.done), React.createElement(Input, {
            onSubmit: this.addItem
        }));
    }
});

React.render(React.createElement(Container, {
    model: model
}), document.getElementById("app"));